#!/usr/bin/env sh
# Bloqueia commit com lint/type errors.
cd campanha-ia || exit 1
npx tsc --noEmit || {
  echo "✗ TypeScript errors — commit abortado"
  exit 1
}
npx lint-staged --cwd campanha-ia || exit 1
